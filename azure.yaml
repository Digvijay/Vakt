# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: vakt
metadata:
  template: aspire-starter-csharp@0.0.1-beta
services:
  app:
    project: src/ProjectVakt.AppHost/ProjectVakt.AppHost.csproj
    language: dotnet
    host: containerapp
